name: Dana Silver

exclude: ['less',
          '.gitignore',
          'README.md',
          'js',
          'Gemfile']

permalink: pretty

markdown: redcarpet
redcarpet:
    extensions: ['fenced_code_blocks', 'autolink', 'tables', 'no_intra_emphasis']

highlighter: pygments